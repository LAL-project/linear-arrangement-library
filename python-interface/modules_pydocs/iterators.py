r"""

Iterators module
----------------

This module contains classes useful for iterating through the graph. These classes iterate

- over the set of edges (see ``E_iterator``).
- over the set of pairs of independent edges :math:`Q` (see ``Q_iterator``).

"""
