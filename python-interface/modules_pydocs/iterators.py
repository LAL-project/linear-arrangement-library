r"""

Iterators module
----------------

This module contains classes useful for iterating through the graph. These classes iterate

- over the set of edges (see ``lal.iterators.E_iterator``).
- over the set of pairs of independent edges :math:`Q` (see ``lal.iterators.Q_iterator``).

"""
