r"""

Version module
---------------

This module contains the version of this build.

See the variables

- major -- The year in which this build was released (the last two digits of the year),
- minor -- The month in which this build was released (as a two-digit number),
- patch -- Bug fix number: this indicates a build in which only bugs were fixed (i.e., no new features, or other changes were made). This is also a two-digit number.

"""
